"""Project service scripts (migrations, backups, health checks).

Console output of these scripts is ASCII-only: Windows consoles decode program
output using the system code page, so non-ASCII text would be displayed as
garbage.
"""
